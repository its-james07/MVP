document.addEventListener('DOMContentLoaded', function(){
    const form = document.getElementById('changePasswordForm');
    form.addEventListener('submit', ()=>{
        const changeBtn = form.querySelector('button[type="submit"]');
    })
})